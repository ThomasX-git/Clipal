## v0.4.3

- 新增 `clipal service`：对接系统服务管理器实现后台运行的 install/start/stop/restart/status/uninstall
  - macOS：launchd（LaunchAgent + `launchctl`）
  - Linux：systemd user service（`systemctl --user`）
  - Windows：任务计划程序（`schtasks.exe`，登录后启动）
- 文档更新：各平台指南补充 `clipal service *` 推荐用法与 `--force` 覆盖安装

